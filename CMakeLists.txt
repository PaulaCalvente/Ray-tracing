cmake_minimum_required(VERSION 3.10)
project(Ray_tracing)
add_executable(principal main.cpp)
find_package(OpenMP REQUIRED)
if(OpenMP_CXX_FOUND)
    target_link_libraries(principal PUBLIC OpenMP::OpenMP_CXX)
endif()